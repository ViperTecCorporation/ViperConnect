import { type BaileysLikeSocket } from './capabilities';
import type { ViperVoiceProvisionResponse, ViperVoiceRoutingMode } from './protocol';
export type ViperVoiceLogger = Pick<Console, 'debug' | 'info' | 'warn' | 'error'>;
export type ViperVoiceAdapterOptions = {
    sock: BaileysLikeSocket;
    serviceUrl?: string;
    bridgeUrl?: string;
    provisionUrl?: string;
    provisionToken?: string;
    bridgeToken?: string;
    slotId?: string;
    phoneNumber?: string;
    software?: string;
    instanceId: string;
    displayName?: string;
    companyId?: string;
    accountId?: string;
    routingMode?: ViperVoiceRoutingMode;
    lineGroupId?: string;
    extensionGroupId?: string;
    extensionId?: string;
    selfJid?: string;
    selfLid?: string;
    autoConnect?: boolean;
    logger?: ViperVoiceLogger;
};
export type ViperVoiceAdapterHandle = {
    slotId?: string;
    provision?: ViperVoiceProvisionResponse;
    connect(): Promise<void>;
    close(): void;
    isConnected(): boolean;
};
export declare class ViperVoiceAdapter implements ViperVoiceAdapterHandle {
    private readonly options;
    slotId?: string;
    provision?: ViperVoiceProvisionResponse;
    private readonly adapter;
    private readonly logger;
    private readonly capabilities;
    private readonly library;
    private ws?;
    private removeListeners;
    private connected;
    constructor(options: ViperVoiceAdapterOptions);
    connect(): Promise<void>;
    close(): void;
    isConnected(): boolean;
    private resolveBridgeUrl;
    private provisionSlot;
    private openBridge;
    private attachBaileysListeners;
    private sendEvent;
    private handleBridgeMessage;
    private handleRpc;
}
export declare const useViperVoiceBaileys: (options: ViperVoiceAdapterOptions) => Promise<ViperVoiceAdapterHandle>;
